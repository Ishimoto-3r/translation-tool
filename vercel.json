{
    "buildCommand": "echo 'No build needed for static files'",
    "outputDirectory": ".",
    "functions": {
        "api/*.js": {
            "maxDuration": 60
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/$1"
        }
    ]
}